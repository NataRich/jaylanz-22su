import { ContainerProps } from './container'

const FooterContainer = ({ children }: ContainerProps) => {
  return <div className="footer-container h-centered">{children}</div>
}

export default FooterContainer
